#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
CONFIG_FILE="$ROOT/demo/server.toml"

cd "$ROOT"

exec cargo run -p nullpoint-server -- --config "$CONFIG_FILE"
